const burgerMenu = document.querySelector('.menu-burger');
const mobile-nav = document.querySelector('.nav-links');

burgerMenu.addEventListener('click', () = {
    mobile-nav.classList.toggle('open');
    burgerMenu.classList.toggle('open');
});

   